#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main() {
  // ifstream input ("input.txt");
  ifstream input("test.txt");
  string line;
  while (getline(input, line)) {
  }
  return 0;
}
